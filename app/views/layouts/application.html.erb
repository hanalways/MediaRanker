<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag asset_path('favicon.ico') %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
    <h1>
      <%= link_to "Media Ranker", root_path %>
      <sub>Ranking the Best of Everything</sub>
    </h1>

    <nav class="navbar navbar-light bg-light">
      <form class="form-inline">
          <%= link_to "View top media", root_path, class: "btn btn-link" %>
          <%= link_to "View all media", works_path, class: "btn btn-link" %>
          <%= link_to "Add a new work", new_work_path, class: "btn btn-link" %>
          <%= link_to "View all users", root_path, class: "btn btn-link" %>
      </form>

      <div class="login">
        <%=link_to "Login", login_path, class: "btn btn-link" %>
        <%# BUILD LOGIC FOR LOG IN/LOG OUT %>
      </div>
    </nav>
  </header>

  <section>
    <%= yield %>
  </section>
  </body>
</html>
